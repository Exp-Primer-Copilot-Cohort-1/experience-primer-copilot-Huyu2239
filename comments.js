// Create web server
// Create web server
var express = require('express');
var app = express();
var port = 3000;
// Use the Express middleware to serve up static files
app.use(express.static('public'));
// Start the server
app.listen(port, function() {
  console.log('app started');
});
// Use the Express middleware to serve up static files
app.use(express.static('public'));

// Path: comments.js
// Create web server
var express = require('express');
var app = express();
var port = 3000;
// Use the Express middleware to serve up static files
app.use(express.static('public'));
// Start the server
app.listen(port, function() {
  console.log('app started');
});
// Use the Express middleware to serve up static files
app.use(express.static('public'));

// Path: comments.js
// Create web server
var express = require('express');
var app = express();
var port = 3000;
// Use the Express middleware to serve up static files
app.use(express.static('public'));
// Start the server
app.listen(port, function() {
  console.log('app started');
});
// Use the Express middleware to serve up static files
app.use(express.static('public'));

// Path: comments.js
// Create web server
var express = require('express');
var app = express();
var port = 3000;
// Use the Express middleware to serve up static files
app.use(express.static('public'));
// Start the server
app.listen(port, function() {
  console.log('app started');
});
// Use the Express middleware to serve up static files
app.use(express.static('public'));

// Path: comments.js
// Create web server
var express = require('express');
var app = express();
var port = 3000;
// Use the Express middleware to serve up static files
app.use(express.static('public'));
// Start the server
app.listen(port, function() {
  console.log('app started');
});
// Use the Express middleware to serve up static files
app.use(express.static('public'));

// Path: comments.js
// Create web server
var express = require('express');
var app = express();
var port = 3000;
// Use the Express middleware to serve up static files
app.use(express.static('public'));
// Start the server
app.listen(port, function() {
  console.log('app started');
});
// Use the Express middleware to serve
